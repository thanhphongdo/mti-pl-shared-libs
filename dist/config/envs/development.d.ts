export declare const config: {
    env: string;
    db: {
        mongodb: string;
    };
    jwt: {
        secret: string;
        refreshSecret: string;
    };
    ports: {
        gateway: string;
        authentication: string;
        user: string;
    };
    serviceDomain: {
        gateway: string;
        authentication: string;
        user: string;
    };
};
