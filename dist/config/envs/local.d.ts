export declare const config: {
    env: string;
    db: {
        mongodb: string;
    };
    jwt: {
        secret: string;
        refreshSecret: string;
    };
    ports: {
        gateway: number;
        authentication: number;
        user: number;
    };
};
